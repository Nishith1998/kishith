import { ShowAfterDirective } from './show-after.directive';

describe('ShowAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAfterDirective();
    expect(directive).toBeTruthy();
  });
});
