import { RemoveAfterDirective } from './remove-after.directive';

describe('RemoveAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveAfterDirective();
    expect(directive).toBeTruthy();
  });
});
